{
  "schemaVersion": 2,
  "contract": "calligraphy.auth.v2",
  "description": "Nebula Calligraphy 本地认证及托管 Identity 认证契约。",
  "authRequest": {
    "required": [
      "username",
      "password"
    ],
    "fields": {
      "username": "学习者登录名。MVP 会转为小写，且至少要求 3 个字符。",
      "password": "学习者密码。至少要求 8 个字符，服务端只保存 Argon2id 加盐派生哈希。"
    }
  },
  "user": {
    "required": [
      "user_id",
      "username",
      "created_at"
    ],
    "fields": {
      "user_id": "稳定的本地用户 ID，用作草稿、收藏和练习记录的 owner_user_id。",
      "username": "标准化后的展示/登录用户名。",
      "created_at": "用户创建时间戳。"
    }
  },
  "authSession": {
    "required": [
      "token",
      "expires_at",
      "user"
    ],
    "fields": {
      "token": "注册或登录后返回的服务端会话令牌。",
      "expires_at": "RFC3339 会话过期时间，默认有效期 24 小时，可通过 CALLIGRAPHY_SESSION_TTL 调整。",
      "user": "已认证用户对象。"
    }
  },
  "endpoints": {
    "POST /api/v1/calligraphy/auth/register": "创建本地 MVP 账号并返回会话。",
    "POST /api/v1/calligraphy/auth/login": "校验用户名和密码并返回会话。",
    "POST /api/v1/calligraphy/auth/logout": "吊销当前 Bearer 令牌。",
    "GET /api/v1/calligraphy/auth/me": "通过 Authorization: Bearer <token> 解析当前用户。"
  },
  "security": {
    "ownerScopedEndpoints": "草稿、收藏、练习和学习档案接口都要求 Authorization: Bearer <token>。",
    "ownerEnforcement": "有效所属用户来自认证用户。owner_user_id 或路径 owner 与认证用户不匹配时返回 403。",
    "loginLockout": "连续登录失败会触发临时锁定。",
    "managedMode": "managed 模式关闭本地注册、登录和退出端点，只接受 Nebula Identity 令牌；Flutter 客户端默认通过 HTTPS 使用 nebula-direct 获取令牌。",
    "identityValidation": "托管 Identity 令牌必须通过签名、issuer、audience、exp 和 nbf 校验。",
    "audit": "注册、登录、退出、草稿变更、导出、收藏变更和练习事件应写入配置的审计日志。"
  }
}
