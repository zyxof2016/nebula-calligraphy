# Mobile App

Flutter C-side app entrypoint. It should consume `services/calligraphy` APIs for glyph search, layout preview, artwork drafts, and exports.

This directory is intentionally not scaffolded with Flutter code yet; create it when the mobile UI implementation starts.
