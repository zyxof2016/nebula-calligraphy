# Render Package

PDF, PNG, SVG, and template export helpers should live here once export rendering is implemented.

Exports must write checksummed artifacts to object storage and emit audit events before public beta.
